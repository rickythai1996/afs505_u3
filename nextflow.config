#!/usr/bin/env nextflow

profiles {
  slurm {
    process {
      executor = "slurm"
      queue = "ficklin_class"
      clusterOptions =  "--account=ficklin_class"
      time = "24h" 
      
      blast {
              
    
       cpus = 5

        }
      }
    }
  }

